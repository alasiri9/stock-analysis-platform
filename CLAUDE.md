# Algomatix — منصة تحليل الأسهم الأمريكية

## عن المشروع
منصة ويب تعليمية بالعربية (Flask + PostgreSQL) تفحص 32 سهماً أمريكياً وترصد فرص الانطلاق قبل حدوثها. صاحبها أحمد — **مستخدم مبتدئ غير مبرمج**: اشرح له بالعربي البسيط، خطوة بخطوة، بدون مصطلحات تقنية معقدة، ونفّذ طلبه بأصغر تعديل ممكن دون توسيع النطاق.

## ⚠️ قاعدة ملزمة في كل جلسة — التنبيه على الأخطاء
أيّ خطأ — **مني (أحمد) أو من Codex أو من أي جهة** — يجب أن تنبّهني عليه فوراً بهذا الشكل بالضبط:
- ابدأ بعلامة الخطأ الصفراء **⚠️**.
- تحتها **شرح واضح بالعربي البسيط للخطأ** (ما هو، وأين، ولماذا هو خطأ، وما أثره المحتمل).
- ثم **رأيك المهني والتقني والهندسي والاحترافي الصريح** (بلا مجاملة، حتى لو خالف رأيي أو رأي Codex).
- **ثم نتشاور فيه — والقرار النهائي لي أنا (أحمد) وحدي.** لا تنفّذ إصلاحاً أو تجاوزاً قبل قراري.
هذه القاعدة ملزمة ضمن تعليمات المشروع، ولا تتجاوز سياسات الأمان أو الصلاحيات الأعلى أولوية، ولا تُعد بذاتها إذناً لأي تعديل أو إجراء Git أو نشر. عند التعارض، نبّه أحمد وتوقف حتى قراره. (كتمان الخطأ أو تمريره بصمت مرفوض — نبّه أولاً ثم انتظر القرار.)

## 📌 النواقص — ذكّر أحمد بها في بداية كل جلسة
1. **الفحص البصري (Playwright) غير محفوظ في الاختبارات الرسمية** — يعتمد على أداة تُثبَّت مؤقتاً وسكربت غير مُلتزم، فلا يُعاد تشغيله تلقائياً لاحقاً.
2. **معايرة درجة الثقة مالياً** — نواة Data Confidence رُوجِعت واختُبرت برمجياً، لكن الأوزان والحدود **لم تخضع لمعايرة مالية خارجية مستقلة**؛ والواجهة تعرض ما تحسبه النواة كما هو بأمانة.
3. **فرع قديم مهجور على GitHub** (`claude/american-platform-file-19r593` عند `7163e85`) — ملتبس التاريخ وقد يسبّب خلطاً؛ يُفضّل توضيحه أو حذفه لاحقاً بإذن أحمد.

(حدّث هذه القائمة أو احذف المُعالَج منها بإذن أحمد.)

## 🤝 الاتفاقات الفنية المثبّتة (مرجع دائم)
### اتفاق «الإخفاقات الـ26 في الاختبارات» (أحمد + Claude + Codex — APPROVED)
الدليل: على `main` عند `6ff24aa` = `26 failed / 274 passed`؛ بعد F2 = `26 failed / 302 passed` (نفس الإخفاقات في الملفات الخمسة نفسها: persistence 17 · logic_audit_phase2 4 · logic_audit_phase4 2 · phase5_baseline 2 · audit_high_v2 1)، ونجحت اختبارات F2 UI الجديدة (28). ⇒ **F2 لم تُضِف أي إخفاق**؛ المشكلة legacy مثبتة في `main`، منفصلة تماماً عن F2.
التفاهم الفني المثبت:
1. **لا** تُعالَج الإخفاقات الـ26 داخل فرع F2 الحالي.
2. **لا** تمنع هذه الإخفاقات إنشاء/إغلاق PR الخاص بـF2.
3. الإصلاح **ليس ملغى**: يبدأ في **فرع مستقل فور إغلاق ونشر F2، وقبل `/changes` أو أي ميزة جديدة**.
4. **معيار الإغلاق — تحقق في PR #5:** نجاح `pytest -q` كاملاً في عملية واحدة + استمرار نجاح كل ملف منفرداً، **دون** حذف/تعطيل/تخفيف أي اختبار، و**دون** تغيير منطق الإنتاج أو العتبات لإرضاء الاختبارات.
5. هذا **توثيق للرأي الفني المتفق عليه فقط** — وليس إذناً بأي تعديل/commit/push/PR/merge/deploy.

### اتفاق «اختبارات Playwright الرسمية» (أحمد + Claude + Codex — AGREE مع تصحيحين)
الوضع: الفحص البصري نُفّذ فعلاً (مكتب/جوال × داكن/فاتح + hover/tap + منع الانتقال + LTR + التباين + unavailable/missing/caps + عدم overflow)، لكنه بقي **سكربتاً ولقطات مراجعة مؤقتة** (مؤرشفة خارج المستودع)، **لا اختبارات رسمية**. تغطية منطق F2 مضمونة رسمياً في `tests/test_phase6_ui_wiring.py`.
التفاهم الفني المثبت:
1. **لا** يُضاف Playwright إلى فرع F2 الحالي (تبعية + متصفح + تغيير CI = توسيع نطاق ومخاطرة على ميزة جاهزة).
2. تحويل أهم فحوص Playwright إلى **اختبارات رسمية قابلة للتكرار** = عمل مستقل لاحق (دَين تقني مؤكّد، لا يُهمَل).
3. **تصحيح Codex 1:** إصلاح عزل الـ26 قبل Playwright ترتيب مشروع أفضل، **لا شرط تقني مطلق** (يمكن عزل Playwright في مسار مستقل).
4. **تصحيح Codex 2:** الصياغة الدقيقة = **«لا مانع تقني داخل STEP 1–3 من الانتقال إلى إجراءات ما قبل PR»** — وليست «لا مانع للنشر الآن». النشر يبقى بعد: تصحيح CLAUDE.md → مراجعة → فحص نطاق PR → PR (بإذن) → مراجعة ما قبل الدمج → merge/نشر (بإذن مستقل).
5. **معيار الإغلاق لاحقاً:** تغطية المكتب/الجوال/الثيمين + hover/tap + منع الانتقال + LTR/التباين/unavailable-missing-caps + عدم overflow + أمر تشغيل موحّد موثّق + بلا لقطات/تبعيات زائدة في المستودع.

### التسلسل المعتمد لإغلاق F2 (أحمد + Claude + Codex)
1. تصحيح صياغات CLAUDE.md الثلاث → 2. مراجعة commit التصحيح → 3. فحص نطاق PR → 4. إنشاء PR (بإذن أحمد) → 5. مراجعة ما قبل الدمج → 6. merge/نشر (بإذن مستقل) + تحقّق الموقع → 7. إصلاح عزل الـ26 (فرع مستقل) → 8. تحويل فحوص Playwright إلى اختبارات رسمية قابلة للتكرار (عمل مستقل). البنود 7–8 **قبل `/changes` أو أي ميزة جديدة**.

**الحالة الآن (بعد إغلاق البند 7):** البنود 1–7 ✅ مُنجَزة — F2 دُمجت ونُشرت (merge commit `f5fa8cbd76f4531f3801fcccc6feb3684dca3ce4`، Railway `success`، تحقّق حيّ ناجح)، وأُصلح عزل الاختبارات في PR #5 (fixture في `tests/conftest.py`): `pytest -q` يعطي الآن **330 passed** في عملية واحدة مع استمرار نجاح الملفات الخمسة منفردة، **دون** تغيير إنتاج أو حذف/تعطيل أي اختبار. **الإجراء التالي = البند 8: تحويل فحوص Playwright إلى اختبارات رسمية قابلة للتكرار.** يبقى `/changes` وأي ميزة جديدة مؤجّلين حتى إغلاق البند 8.

## الموقع الحي
- الرابط: https://algomatix-production.up.railway.app
- الاستضافة: Railway (مشروع blissful-imagination) — **أي دمج (merge) في فرع main يُنشر تلقائياً على الموقع الحي خلال دقيقتين**. لا تدمج تعديلاً غير متأكد منه.
- متغيرات البيئة على Railway: FMP_API_KEY, FINNHUB_API_KEY, DATABASE_URL, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, APP_PASSWORD (حماية دخول المنصة).

## البنية
- `app.py` — كل المسارات (routes) + إعداد التطبيق + حماية كلمة المرور (before_request).
- `models.py` — الجداول: StockCache, Signal, PricePoint, PortfolioHolding, Watchlist.
- `services/` — المنطق: `screener.py` (الماسح والإشارات، UNIVERSE = 32 سهماً)، `indicators.py` (المؤشرات الفنية)، `scoring.py` (Piotroski + Catalyst)، `analysis.py` (تقرير السهم + الشارت)، `radar.py` (مطلعو EDGAR)، `news_client.py` (أخبار Finnhub)، `scheduler.py` (تحديث ليلي 01:00 UTC + تقرير تلغرام صباحي)، `telegram_client.py`، `fmp_client.py`، `finnhub_client.py`، `edgar_client.py`.
- `templates/` — قوالب Jinja بثيم داكن (aurora). البطاقة المشتركة: `_scard.html`.
- `static/style.css` — كل التنسيقات. **مهم: عند أي تعديل CSS زد قيمة `?v=` في `base.html` لكسر الكاش** (مثال: v=20260703m → v=20260705a).

## قيود مهمة
- **FMP المجاني: 250 طلباً/يوم فقط** (كل سهم يستهلك ~6 طلبات). لا تضف رموزاً جديدة للـUNIVERSE بدون اختبار توفرها بالباقة المجانية أولاً — كثير من الرموز الكبيرة محجوبة (تعطي 402).
- Finnhub المجاني يمنع endpoint الشموع (403) — ATR يُحسب من أسعار FMP التاريخية.
- التحديث الليلي تلقائي (01:00 UTC) — لا حاجة لأزرار تحديث يدوية غالباً.
- النبرة في النصوص تعليمية لا تنبؤية (لا وعود بأرباح).
- كل النصوص بالواجهة عربية، والاتجاه RTL.

## قواعد العمل
- التزم بأسلوب الكود والتسميات الموجودة؛ أعد استخدام الكلاسات (.panel, .pill, .insider, .stat-row) بدل إنشاء CSS جديد.
- رسائل الـcommit بالعربية بنفس أسلوب السجل الحالي.
- اختبر محلياً ما أمكن قبل اقتراح الدمج، ووضّح لأحمد بالعربي ماذا تغيّر بالضبط.
- **قبل أي دمج في main: شغّل `python tests/smoke_test.py`** — يتأكد أن كل الصفحات ترجع 200 (لا يستهلك باقة، على قاعدة SQLite مؤقتة). لا تدمج لو رجع ✗.
