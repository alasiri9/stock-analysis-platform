[pytest]
# `pytest -q` بلا وسيط = بوابة الوحدة/الانحدار فقط (tests/) = 330 passed.
# اختبارات E2E في e2e/ تُشغَّل صراحةً: `python -m pytest e2e` أو scripts/run_e2e.sh
# (معزولة كي لا تتطلب متصفحاً في بوابة الوحدة، وبلا skip/xfail).
testpaths = tests
