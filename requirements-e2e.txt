# تبعيات اختبارات E2E فقط (Playwright) — منفصلة عمداً عن requirements.txt
# كي لا تحمّل صورة الإنتاج/Railway متصفحاً. تُثبّت في بيئة معزولة:
#   python -m pip install -r requirements.txt -r requirements-e2e.txt
#   python -m playwright install --with-deps chromium
#
# الإصدارات مقفلة ومُتحقَّق من تشغيلها معاً (بلا تعارض resolver) تحت:
#   Python 3.11.15  ·  المجموعة القديمة تبقى 330 passed.
playwright==1.62.0
pytest-playwright==0.9.0
pytest==9.1.1
