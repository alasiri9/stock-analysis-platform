# 📒 المنصة الأمريكية — ذاكرتي الكاملة (من الألف للياء)

> **لأحمد:** هذا ملفك الوحيد للذاكرة، مشترك بين الجوال والكمبيوتر عبر GitHub.
> في أي محادثة على **claude.ai/code** (جوال أو كمبيوتر)، اكتب:
> **«اقرأ ملف المنصة الأمريكية»** — وبأتذكر كل اللي تحت فوراً.
> ولإضافة أي شي، قول: **«سجّل في المنصة الأمريكية: ...»** وأنا أكتبه وأرفعه لك.
>
> ⚠️ يشتغل عبر **claude.ai/code** فقط (المتصفح) — مو تطبيق Claude من المتجر.

---

## 🧩 1) نبذة عن المشروع
منصة ويب **تعليمية** بالعربية تفحص وتحلّل **32 سهماً أمريكياً** وترصد فرص الانطلاق قبل حدوثها.
- **الهدف:** أداة فحص وتحليل تعليمية فقط — **لا توصيات ولا تنبؤات ولا وعود أرباح**.
- **صاحبها:** أحمد — مستخدم مبتدئ غير مبرمج، يفضّل شرحاً عربياً بسيطاً خطوة بخطوة وكلاماً قليلاً.

## 🧱 2) التقنية والبنية
- **الواجهة الخلفية:** Python + Flask
- **قاعدة البيانات:** PostgreSQL (عبر SQLAlchemy)
- **الاستضافة:** Railway (مشروع blissful-imagination) — **أي دمج في فرع `main` يُنشر تلقائياً على الموقع الحي خلال دقيقتين**.
- **الموقع الحي:** https://algomatix-production.up.railway.app (محمي بكلمة مرور APP_PASSWORD).
- **مستودع GitHub:** alasiri9/stock-analysis-platform

## 🔌 3) مصادر البيانات (من أين تجي الأرقام)
- **FMP** (Financial Modeling Prep) → بيانات مالية + سعر + تاريخ أسعار (لحساب ATR).
- **Finnhub** → سعر لحظي (مصدر تأكيد ثانٍ يرفع درجة الثقة).
- **SEC EDGAR** → معاملات المطّلعين (insiders) — بدون مفتاح.
- متغيرات البيئة على Railway: FMP_API_KEY, FINNHUB_API_KEY, DATABASE_URL, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, APP_PASSWORD.

## 🗄️ 4) جداول قاعدة البيانات (models.py)
- **StockCache** — (ticker, data_json, updated_at) نتائج الفحص المخزّنة.
- **Signal** — (ticker, signal_type, triggered_at, price_at_signal) الإشارات.
- **PricePoint** — (ticker, date, price) تاريخ الأسعار.
- **PortfolioHolding** — (ticker, shares, buy_price, user_id) المحفظة.
- **PortfolioSnapshot** — (date, total_cost, total_value) لقطات أداء المحفظة.
- **Watchlist** — (ticker, user_id, added_at, added_price) قائمة المتابعة.

## 📄 5) الصفحات (الروابط داخل الموقع)
- `/` — **الرئيسية**: الماسح + الفلاتر (Piotroski، النمو/Catalyst، السعر، القطاع، عدد المقاييس المجتمعة).
- `/stock/<رمز>` — **تقرير السهم**: سعر + مقاييس مالية + Piotroski + Catalyst + خطة ATR + مطّلعين + شارت.
- `/gems` — **الجواهر** • `/leaders` — **القادة** • `/prelaunch` — **ما قبل الانطلاق**.
- `/signals` — **الإشارات** • `/daily-report` — **التقرير اليومي**.
- `/radar` — **رادار المطّلعين** (EDGAR) • `/news` — **الأخبار** (Finnhub) • `/flow` — **التدفق**.
- `/performance` — **الأداء** • `/compare` — **المقارنة** • `/calculator` — **حاسبة**.
- `/watchlist` — **قائمة المتابعة** • `/portfolio` — **المحفظة** • `/login` `/logout`.
- `/learn` — **📚 تعلّم** (قاموس مصطلحات) • `/earnings` — **📆 رزنامة الأرباح** • `/alerts` — **🔔 تنبيهات سعرية**.

## 🧮 6) الحسابات (scoring.py + indicators.py)
- **Piotroski Score** (0–9) — محسوب يدوياً من FMP، بتفصيل كل نقطة.
- **Catalyst Score** (0–100) — سُمّي في الواجهة **«النمو»** (تمييزاً عن زخم السعر MACD). أيقونة القوة: 🟢 ≥80 / 🟡 40–79 / 🔴 <40.
- **خطة ATR تعليمية** — دخول / وقف / هدف (حساب تعليمي لا توصية). ATR يُحسب من أسعار FMP التاريخية لأن Finnhub المجاني يمنع الشموع (403).

## 🤖 7) الأتمتة (scheduler.py)
- **تحديث ليلي تلقائي 01:00 UTC** لكل الأسهم — غالباً لا حاجة لأزرار تحديث يدوية.
- **تقرير تلغرام صباحي** يُرسل تلقائياً.

## 🌐 8) قائمة الأسهم (UNIVERSE = 32 سهماً)
AAPL, MSFT, NVDA, GOOGL, AMZN, META, TSLA, AMD, NFLX, JPM, V, WMT, UNH, JNJ, XOM, ADBE, COST, CVX, KO, PEP, DIS, INTC, BAC, PYPL, ABBV, PFE, CSCO, VZ, T, WFC, GS, NKE.

## ⛔ 9) المبادئ الجوهرية (لا نحيد عنها)
1. **لا رقم بدون داتا حقيقية** — أي قيمة غير متوفرة تُعرض كـ «—» (لا صفر ولا رقم ملفّق).
2. **None ≠ 0** — نفرّق بوضوح بين بيانات غائبة وصفر حقيقي.
3. **Unit Guards إلزامية** — FMP يعيد نسباً مئوية (ROE=15.3 = 15.3%)، Finnhub بعضها كسور (0.153) — نعاير كل حقل مع تعليق يوضّح المصدر والوحدة.
4. **لا لغة توصية أو تنبؤ** — شاشة فحص تعليمية فقط.
5. كل النصوص عربية والاتجاه RTL.

## ⚠️ 10) قيود مهمة أتذكرها
- **FMP المجاني: 250 طلباً/يوم فقط** (كل سهم ~6 طلبات) — لا نضيف رموزاً جديدة بدون اختبار توفرها بالباقة المجانية (كثير من الرموز الكبيرة محجوبة = 402).
- عند أي **تعديل CSS**: نزيد قيمة `?v=` في `templates/base.html` لكسر الكاش.
- الثيم داكن (aurora)، والبطاقة المشتركة `templates/_scard.html` — نعيد استخدام الكلاسات الموجودة (.panel, .pill, .insider, .stat-row) بدل CSS جديد.
- رسائل الـcommit بالعربية بنفس أسلوب السجل.

## ✅ 11) الحالة العامة
المنصة **مكتملة ومنشورة وشغّالة** على Railway. آخر التعديلات كانت على فلاتر الرئيسية وأيقونات قوة النمو وتوسيط العناوين.

---

## 📝 12) ملاحظاتي وأفكاري (نضيف عليها مع الوقت)
- **2026-07-08:** أضفنا 4 تطويرات ونشرناها على الموقع الحي:
  1. **📚 تعلّم** — قاموس مصطلحات مبسّط.
  2. **📆 رزنامة الأرباح** — مواعيد الأرباح القادمة مرتّبة بالأقرب.
  3. **📈 رسم مصغّر (sparkline)** على كل بطاقة سهم.
  4. **🔔 تنبيهات سعرية** — تضيف "نبّهني إذا نزل/صعد سهم لسعر" ويجيك تنبيه تلغرام (يُفحص مرة يومياً بعد التحديث الليلي — الباقة المجانية لا تسمح بفحص لحظي).
  - جدول جديد: `PriceAlert` (يُنشأ تلقائياً عبر db.create_all).
- **2026-07-08 (دفعة ثانية):** أضفنا 4 تحسينات UX ونشرناها:
  1. **أزرار فلترة جاهزة** بالرئيسية (جواهر النمو/نمو قوي/لسا ما صعد/أقل من 100$/مسح).
  2. **أيقونة «؟»** جنب فلترَي الجودة والنمو تربط بشرح المصطلح في «تعلّم».
  3. **قسم "أرباح وشيكة خلال يومين"** في تقرير تلغرام الصباحي.
  4. **جملة "💡 سبب الترشّح"** (أقوى 3 عوامل) على بطاقة السهم.
- **2026-07-08 (دفعة ثالثة):** غيّرنا **الرقم الكبير في بطاقة السهم**: صار يمثّل **عدد المقاييس المجتمعة (قوة التأكيد)** بدل درجة النمو، وتحته كلمة «قوة التأكيد». درجة النمو انتقلت للشارة الصغيرة (🚀 نمو). وأضفنا شرح «قوة التأكيد» في صفحة «تعلّم» (id=confidence).
- **2026-07-09:** تناسق + ترتيب:
  1. صفحة السهم: بطاقة «قوة التأكيد» + أيقونات «؟» على الدرجات + جملة «سبب الترشّح» (تُجلب من سجل الماسح لنفس السهم).
  2. الرئيسية: ترتيب النتائج حسب قوة التأكيد/النمو/السعر (قائمة `sort` داخل لوحة الفلاتر).
  3. تحققنا من الجوال: لا تجاوز أفقي في أي صفحة (كل العناصر الجديدة تلتفّ صح).
- **2026-07-09 (عدّاد):** أضفنا **عدّاد نصف دائري متحرّك (سبيدومتر)** بإبرة تلف من 0 للقيمة بألوان أحمر→أصفر→أخضر. قالب مشترك `_gauge.html`. في البطاقة: عدّاد «قوة التأكيد» (من 16). في صفحة السهم: عدّاد قوة التأكيد (16) + عدّاد النمو Catalyst (100). يحترم prefers-reduced-motion.
- **2026-07-09 (لمسات أخيرة):**
  - أضفنا عدّاداً لبطاقة **Piotroski** في صفحة السهم (من 9) — فصار فيها 3 عدّادات متناسقة (Piotroski/قوة التأكيد/النمو).
  - فصلنا **زرّي التحديث** («تحديث شارت الأسهم» + «تحديث البيانات») في صندوق مستقل «🔧 أدوات التحديث»، وبقي زر «فلترة» داخل لوحة الفلاتر.

---

- **2026-07-09 (بار السوق):** أضفنا **باراً لاتجاه السوق** أسفل بانر S&P 500 — متمركز في الصفر: أخضر يميناً (صاعد) / أحمر يساراً (هابط)، طوله حسب حركة المؤشر خلال شهر، وتحته تسميات هابط/ثبات/صاعد. (آخر CSS: v=20260709f)

## 🗂️ حالة آخر لقاء (2026-07-09)
كل ما تحت **منشور على الموقع الحي ومختبَر**:
- أقسام جديدة: 📚 تعلّم • 📆 رزنامة الأرباح • 🔔 تنبيهات سعرية.
- تحسينات: أزرار فلترة جاهزة • أيقونة «؟» للشرح • تنبيه أرباح وشيكة بتلغرام • جملة «سبب الترشّح» • ترتيب النتائج (قوة التأكيد/النمو/السعر).
- البطاقة: الرقم الكبير = **قوة التأكيد** (عدد المقاييس المجتمعة من 16)، والنمو انتقل للشارة الصغيرة.
- **عدّادات متحركة (سبيدومتر)**: بالبطاقة (قوة التأكيد) وصفحة السهم (Piotroski + قوة التأكيد + النمو).
- صندوق «أدوات التحديث» منفصل عن الفلترة.
- **🧠 ملخّص ذكي للسهم** (نقاط قوة + تنبيهات، مُولّد آلياً) أعلى صفحة السهم.
- **بار اتجاه السوق** (متمركز في الصفر) أسفل بانر S&P 500.
- إعادة تسمية: «ماسح الانطلاق» → «الاستعداد للانطلاق» • «الأسهم التي انطلقت» → «أداء الأسهم التي انطلقت».
- آخر نسخة CSS: **v=20260709f**.

**نقاش مفتوح للمرة الجاية:** ناقشنا تخفيف عبارة «أوزان افتراضية غير مُختبرة تاريخياً» لصياغة ألطف (مثل «مقياس مساعد — اجتهادي للإرشاد») — لم نعدّلها بعد.

**2026-07-09 (الملخّص الذكي):** أضفنا **«🧠 ملخّص ذكي للسهم»** — فقرة عربية مُولّدة آلياً أعلى صفحة السهم (نقاط قوة + تنبيهات)، تجمّع Piotroski/النمو/الاتجاه/RSI/القوة النسبية/السيولة/قرب الأرباح/التقييم في جُمل مبسّطة. دالة `analysis.smart_summary(report, scan)`. وصفي تعليمي، بلا API. استبدل صندوق «سبب الترشّح» القديم. (آخر CSS: v=20260709e)

**2026-07-09 (دفعة كبيرة — نُفّذت كل الأفكار المؤجّلة + المعلّقات):**
- **معلّقات:** «ثبات» → «بلا تغيّر» في بار السوق • خفّفنا عبارة أوزان النمو إلى «📊 مقياس مساعد — اجتهادي للإرشاد».
- **A) تمييز البطاقات القوية:** توهّج أخضر للبطاقات ذات قوة تأكيد ≥10 من 16.
- **C) التغيّر اليومي %:** يظهر جنب سعر السهم بالبطاقة (أخضر/أحمر) — أضفنا `change_percent` لسجل الماسح (يظهر بعد أول تحديث ليلي).
- **B) أسهم من نفس القطاع:** قسم بصفحة السهم يعرض أسهماً من نفس القطاع (من الكاش).
- **D) ربط الحاسبة بـATR:** زر «احسب كمية الشراء» بصفحة السهم يفتح الحاسبة معبّأة بالدخول/الوقف (عبر params في الرابط).
- **E) تقرير أسبوعي بتلغرام:** كل سبت — ملخّص أداء الإشارات (`_send_weekly_report` في scheduler.py).
- **F) محاكاة «لو تابعت الإشارات»:** بصفحة الأداء — استثمار افتراضي 1000$ عند كل إشارة → القيمة الحالية والربح/الخسارة.
- آخر نسخة CSS: **v=20260709i**.

**2026-07-09 (دفعة إضافية — 4 ميزات):**
- **🔔 تنبيه «جاهز للانطلاق»:** تلغرام فوري عند دخول سهم قائمة الاستعداد (نوع إشارة `prelaunch_ready`، دالة `notify_new_prelaunch` في المجدول الليلي).
- **⚙️ صفحة «كيف تعمل المنصة»** (`/how`): شرح المنهجية المبسّط + رابط بالقائمة.
- **🔗 زر مشاركة السهم:** navigator.share أو نسخ الرابط.
- **☀️ وضع فاتح:** زر تبديل بالسايدبار + حفظ بـlocalStorage + سكربت مبكّر يمنع الوميض. متغيّرات `html.light` في style.css.
- آخر نسخة CSS: **v=20260709j**.

**2026-07-09 (دفعة إضافية #2 — 3 ميزات):**
- **📊 صفحة «الرابحون والخاسرون»** (`/movers`): أعلى/أدنى 5 بالتغيّر اليومي من الـ32 (من الكاش) + رابط بالقائمة.
- **⚖️ تطوير المقارنة:** عدّادات (Piotroski + النمو) لكل سهم + بطاقات نقاط قوة/تنبيهات (smart_summary الخفيف، بلا API إضافي).
- **📝 ملاحظاتي على السهم:** جدول `StockNote` + مربع حفظ بصفحة السهم (تفريغ الملاحظة يحذفها).
- آخر نسخة CSS: **v=20260709m**.

**⚠️ تنبيه تقني:** `_scard.html` يفترض أن سجل السهم فيه `piotroski` — لا تزرع بيانات اختبار ناقصة في StockCache محلياً (تكسر الرئيسية 500). السجلات الحقيقية دائماً كاملة.

**2026-07-09 (جودة + 3 ميزات):**
- **جولة جودة:** CSS نظيف (لا تكرار حقيقي)، الوضع الفاتح يغطّي المتغيّرات المهمة، الجوال بلا تجاوز في الصفحات الجديدة.
- **🔎 اقتراح الرموز:** datalist بـ32 سهماً في بحث الرئيسية (UNIVERSE متغيّر عام للقوالب).
- **📋 صفحة «ملاحظاتي»** (`/notes`): تجمّع كل ملاحظات المستخدم + رابط بالقائمة.
- **🖨️ طباعة تقرير السهم:** زر + `@media print` (يخفي القوائم، خلفية بيضاء لتوفير الحبر).
- آخر نسخة CSS: **v=20260709n**.

**2026-07-09 (الإعدادات):** أضفنا **صفحة ⚙️ الإعدادات** (`/settings`) — تتحكّم بـ: المظهر (فاتح/داكن)، الترتيب الافتراضي للنتائج، مبلغ محاكاة الإشارات، وروابط إدارة سريعة. تُحفظ الخيارات في **localStorage** (لكل متصفح). الترتيب الافتراضي يُطبَّق على الرئيسية، ومبلغ المحاكاة على صفحة الأداء (عبر `?amount=`). كبّرنا خط صفحة «كيف تعمل» وحذفنا عبارة «أدوات التحديث». آخر CSS: **v=20260709p**.

**2026-07-09 (نظام الاشتراكات):** أضفنا **إدارة اشتراكات المستخدمين** في الإعدادات (للمدير فقط):
- جدول `Subscriber` (اسم + `access_code` فريد + start_date/end_date).
- الدخول (`/login`) يقبل كلمة مرور المدير (`APP_PASSWORD`, role=admin) أو **رمز مشترك ساري** (role=sub). ينتهي دخول المشترك تلقائياً بعد `end_date` (فحص في `before_request`).
- الإعدادات: إنشاء مشترك بمدة (أيام) → يولّد رمزاً، + تمديد +30، + حذف. محمي بـ`is_admin()` (المشترك لا يرى/لا يصل الإدارة).
- المدير يعطي المشترك **رمز الدخول** ليدخل به بدل كلمة المرور. آخر CSS: **v=20260709q**.
- **مهم:** المشتركون محفوظون بقاعدة البيانات (يعملون من أي جهاز)، بعكس إعدادات المظهر/الترتيب (localStorage محلي).
- **إضافات الاشتراكات:** زر نسخ الرمز 📋 • تمييز «ينتهي قريباً» (≤3 أيام) في جدول المدير • **شريط تنبيه للمشترك نفسه** عند اقتراب انتهائه (≤7 أيام، عبر context_processor). آخر CSS: **v=20260709s**.

**2026-07-09 (لمسات واجهة):**
- **👁 تلميح تفاعلي بكل عنصر قائمة:** أيقونة عين في نهاية كل عنصر (22)، تمرّر عليها الماوس فيظهر شرح مهمة الصفحة (position:fixed بموضع ذكي يمين/يسار، JS في base.html).
- **ألوان الشارات:** خلفية شارة «📅 أرباح قريبة» (`.scard-earn.soon`) ومربع «📉 أداء سلبي منذ الإشارة» (`.lcard-tag-pill.down`) صارت **خضراء فاتحة شفافة** مع إبقاء النص أحمر.
- آخر نسخة CSS: **v=20260709w**.

**2026-07-10 (ذكاء المنصة — 3 ميزات):**
- **🩺 لوحة «صحة المنصة»** (`/health`, للمدير فقط): آخر تحديث ليلي + نضارة الأسهم + تلغرام + المشتركون + إحصائيات القاعدة (بطاقات ملوّنة). `is_admin` صار متاحاً للقوالب.
- **💓 نبض السوق التاريخي** (`/pulse`): جدول `MarketMoodSnapshot` (لقطة يومية بالمجدول) + رسم خطي لنسبة الأسهم الصاعدة عبر الأيام.
- **⚡ دقة الإشارة تاريخياً:** عمود بصفحة الإشارات يعرض نسبة نجاح كل نوع إشارة (من `type_stats`).
- آخر نسخة CSS: **v=20260710a**.

**2026-07-10 (ميزات المشتركين — 3):**
- **📢 إعلان المدير:** جدول `AppSetting` (key/value) + بانر أزرق يظهر لكل المستخدمين + نموذج بالإعدادات (مسح النص يلغيه). مبثوث عبر context_processor.
- **👀 آخر دخول للمشترك:** عمود `last_login` على `Subscriber` — **هجرة آمنة** (`ALTER TABLE ... ADD COLUMN` في try/except عند الإقلاع، لأن create_all لا يعدّل جداول قائمة). يُسجَّل عند دخول المشترك ويظهر بجدول المدير.
- **📥 تصدير Excel:** `/export/scanner.csv` (BOM عربي، مرتّب بقوة التأكيد) + زر «تصدير Excel» بصندوق أدوات الرئيسية.
- آخر نسخة CSS: **v=20260710b**.
- **درس مهم:** إضافة عمود لجدول موجود على الإنتاج يحتاج هجرة يدوية (create_all لا يكفي) — استخدمنا ALTER TABLE آمن عند الإقلاع.

**2026-07-10 (إدارة كلمة المرور):** أضفنا تغيير كلمة مرور المدير **من المنصة** + استعادة عبر تلغرام:
- تغيير من الإعدادات → تُخزّن **مشفّرة** (werkzeug) في `AppSetting['admin_password_hash']`.
- الدخول يقبل: الكلمة المخزّنة **أو** كلمة `APP_PASSWORD` (Railway) كـ**مفتاح طوارئ دائم**.
- استعادة: زر «نسيت كلمة المرور» بصفحة الدخول → يرسل رمزاً (6 أرقام، 10 دقائق، مشفّر، مرة واحدة) لتلغرام المدير → يدخله مع كلمة جديدة.
- مساري `password_forgot`/`password_reset` مُستثناة من حارس الدخول (المستخدم مقفول).
- آخر نسخة CSS: **v=20260710c**.
- **أمان المنصة (فحص سابق):** المفاتيح بـenv (لا GitHub) ✅ • HTTPS ✅ • debug مطفأ بالإنتاج ✅ • ORM يمنع SQLi ✅ • Jinja يمنع XSS ✅. **ثغرة متبقية:** لا حد لمحاولات الدخول (brute force) — لم نضف rate limiting بعد.

**2026-07-10 (حدّ المحاولات):** أضفنا **حدّ محاولات الدخول** — بعد **3 محاولات خاطئة** يُقفل الـIP **5 دقائق** (حتى الكلمة الصحيحة تُرفض أثناء القفل). تتبّع في الذاكرة (`_login_state`) لكل IP (Procfile عامل واحد). ينطبق على الدخول واستعادة الرمز. العدّاد يتصفّر عند النجاح.

**2026-07-10 (تحكّم أمني):** أضفنا 3:
- **🛡️ لوحة «حالة الأمان»** بالإعدادات (للمدير): هل غُيّرت الكلمة الافتراضية (وجود `admin_password_hash`)، حالة الاستعادة، حدّ المحاولات.
- **⚠️ تنبيه اختراق بتلغرام:** عند القفل (3 محاولات فاشلة) يُرسل تنبيه للمدير مع الـIP.
- **🔘 زر تفعيل/إيقاف الاستعادة عبر تلغرام** (`recovery_off` في AppSetting): عند الإيقاف يختفي زر «نسيت كلمة المرور» ويُرفض الطلب.
- آخر نسخة CSS: **v=20260710d**.

**2026-07-10 (CSRF):** أضفنا **حماية CSRF** — `Flask-WTF==1.2.1` + `CSRFProtect(app)` + رمز `{{ csrf_token() }}` مخفي في **كل الـ20 نموذج POST**. اختُبر: بلا رمز يُرفض (400)، مع الرمز يعمل. **سُدّت آخر ثغرة معروفة.**

**🛡️ ملخّص أمان المنصة (مكتمل):** مفاتيح بـenv • HTTPS • debug مطفأ بالإنتاج • ORM يمنع SQLi • Jinja يمنع XSS • صلاحيات (is_admin) • كلمات مرور مشفّرة (werkzeug) • استعادة تلغرام + مفتاح Railway طوارئ • حدّ 3 محاولات + قفل 5د + تنبيه اختراق تلغرام • زر إيقاف الاستعادة • لوحة حالة أمان • **CSRF**. لا ثغرات معروفة متبقية.

**2026-07-11:**
- **تعريب مسميات الدرجات:** Piotroski → «الجودة المالية (Piotroski)» • Catalyst → «النمو (Catalyst)» (صفحة السهم/البطاقة/المقارنة).
- **المؤشرات الفنية المتحققة (فكرة أحمد):** قسم المؤشرات بصفحة السهم صار يعرض **المتحققة (الصاعدة bull) فقط** + شارة نسبة التحقق «X من Y · Z%» (خضراء عند ≥60%). المحايدة والهابطة مخفية حتى تتحقق. `tech` محسوب في مسار stock_report.
- آخر نسخة CSS: **v=20260711a**.
- **«تجميع» بدل «تراكم تجميع»** (OBV): أخفينا كلمة «تراكم» من العرض (المنطق الداخلي label="تراكم" كما هو) + توحيد المصطلح بصفحة تعلّم. وأضفنا **نسبة مئوية للتجميع/التصريف** (صافي تغيّر OBV ÷ إجمالي الحجم) → «تجميع 50%».
- **تفصيل Piotroski المتحقق:** مثل المؤشرات الفنية — نعرض النقاط الناجحة فقط + شارة «X من 9 · Z%». `pio_met` محسوب في المسار.

**2026-07-11 (تحصين للمشتركين):**
- **تخزين تقرير السهم:** كاش 12 ساعة في `StockCache` (بادئة `report:`) → يقلّل طلبات FMP بشدّة. **يصمد وقت انتهاء الحصة** (يعرض المخزّن). السعر اللحظي يُجلب منفصلاً (طلب خفيف) ليبقى حديثاً؛ فشله لا يُسقط التقرير. **الجودة لا تتأثر** (التحليل يتغيّر يومياً/فصلياً، والسعر يبقى حديثاً).
- **مربع حواري ذكي:** حالة عدم توفر السهم صارت **modal** (بدل صفحة/إحالة) بصياغة ودّية للمشترك («قيد التحديث» بلا تفاصيل FMP التقنية) + زر العودة. للأسهم خارج القائمة: «تأكد من الرمز».
- آخر نسخة CSS: **v=20260711b**.
- **للمستقبل التجاري:** الباقة المجانية (250/يوم) لا تكفي لمشتركين كثر — عند نمو الدخل، ترقية FMP لباقة مدفوعة.

**2026-07-11 (تنبيهات منبثقة):** حوّلنا **إعلان المدير + تنبيه قرب انتهاء الاشتراك** من بانرات إلى **مربع حواري منبثق** وسط الشاشة، يظهر **مرة واحدة** (localStorage بمفتاح = محتوى الإعلان + end_date؛ تغيّرهما يُظهره من جديد). أزلنا البانرات القديمة. آخر CSS: **v=20260711c**.

**2026-07-11 (صندوق الرسائل):** حوّلنا الإعلان الواحد إلى **صندوق رسائل (Inbox)**:
- جدول `Message` (رسائل المدير). المدير يرسل من الإعدادات → تُحفظ + تظهر كمربع منبثق (أحدث رسالة، مرة واحدة).
- رابط **«📨 الرسائل»** بالقائمة + **علامة حمراء بعدد غير المقروء** (JS يقارن `msg_ids` بـ`localStorage['lastReadMsgId']`).
- صفحة `/messages` تعرض كل الرسائل (يجد المستخدم ما أغلقه/فاته)؛ فتحها يصفّر العلامة. حذف للمدير.
- آخر CSS: **v=20260711d**. (ملاحظة: `announcement` القديم استُبدل بـ`latest_message`/`msg_ids` في context_processor.)

**2026-07-11 (تبسيط رسالة عدم التوفر):** بسّطنا رسالة المربع الحواري لسهم قيد التحديث (ضمن القائمة) إلى: **«البيانات قيد التحديث — حاول في وقت لاحق.»** بدل النص الطويل.

**2026-07-11 (حذف الملاحظات):** حذفنا **ميزة «📝 ملاحظاتي على السهم»** بالكامل (قرار أحمد: غير ضرورية — تُكتب في مذكرة خارجية). أُزيل: جدول `StockNote` (models.py)، مربع الحفظ بصفحة السهم، مساري `/notes` و`/stock/<t>/note`، روابط القائمة والإعدادات، بطاقة «ملاحظات محفوظة» بصحة المنصة، وقالب `notes.html`. **السبب الأعمق:** الملاحظات كانت مشتركة تحت `guest` واحد (لا خصوصية بين المشتركين). `GUEST_USER` باقٍ لأن قائمة المراقبة/التنبيهات/المحفظة تستخدمه.

**2026-07-12 (فصل خصوصية المشتركين ✅ نُفّذ):** قائمة المراقبة + التنبيهات + المحفظة صارت **خاصة لكل مستخدم**:
- `current_user_id()`: المدير (والوضع المحلي) = `admin`، كل مشترك = `sub:<رقمه>`. حلّت محل `GUEST_USER` في كل مسارات هذه الميزات الثلاث.
- **هجرة آمنة عند الإقلاع** تنقل بيانات `guest` القديمة → `admin` (احتفظ أحمد بكل بياناته).
- اللقطات الليلية (`portfolio.valuation(user_id='admin')`) + التقرير الصباحي + منحنى الأداء التاريخي = محفظة المدير المرجعية (المشترك يرى محفظته وأرقامها لكن بلا منحنى تاريخي خاص).
- ⚠️ **باقٍ:** التنبيهات السعرية تُطلق لتلغرام المدير (لا تلغرام خاص للمشتركين) — كل مشترك يرى قائمة تنبيهاته فقط.

**2026-07-12 (أربع ميزات):**
- **🏠 لوحتي** (`/dashboard`, للجميع): تجمع للمستخدم محفظته + قائمة مراقبته + تنبيهاته النشطة + أحدث 6 إشارات، مع روابط سريعة. من الكاش بلا API.
- **📊 لوحة أعمالي** (`/business`, للمدير): مشتركون نشطون/ينتهون هذا الأسبوع/منتهون + **دخل شهري متوقّع** (نشطون × سعر شهري يُحفظ في `AppSetting['sub_price']` عبر `/business/price`) + جدول من يحتاج تجديداً.
- **⏳ تذكير تجديد بتلغرام:** المجدول الليلي `_notify_expiring_subs()` ينبّه المدير بالمشتركين الذين ينتهون خلال يومين.
- **🎯 حالة تعليمية بالمحفظة:** شارة على كل مقتنى تقارن السعر الحالي بهدف/وقف محسوبَين من سعر الشراء + ATR المخزّن (هدف=شراء+3×ATR، وقف=شراء−1.5×ATR): «بلغ/يقترب من الهدف/الوقف». تعليمي فقط.
- **إصلاح ضمني:** `db` لم يكن مستورداً في كتلة لقطة نبض السوق بالمجدول (كانت `/pulse` لا تتراكم لها لقطات) — أُضيف `from models import db`.
- **ملاحظة:** لا تغيير CSS (أعدنا استخدام `.health-grid/.health-card/.pill/.insider/.set-links/.preset`) — لا حاجة لرفع `?v=`.

**2026-07-12 (تعريب + تبسيط):** حذفنا ميزة الملاحظات نهائياً • بسّطنا رسالة عدم التوفر • «مقتنى» → «سهم» في لوحتي والمحفظة • عرّبنا إشارة `prelaunch_ready` → «🔎 جاهز للانطلاق» • غيّرنا `trend_pullback` إلى «🎯 شراء في الانخفاض» (بالواجهة وتلغرام).

**2026-07-12/13 (إعادة تصميم بطاقة السهم `_scard.html` — دفعة كبيرة بتوجيه أحمد خطوة بخطوة):**
- **الترتيب:** العدّاد يساراً (أكبر) • اسم السهم وأرقامه يميناً • الوسط (المؤشرات + المربع الأخضر + الشارت + السبب) يتمدّد ليملأ ما بينهما.
- **بنية جديدة:** غلّفنا محتوى البطاقة في `.scard-inner` (flex) داخل `.scard` (block). المحتوى محصور بعرض `max-width` متمركز لمنع التبعثر على الشاشات العريضة. **القيمة الحالية `max-width: 1140px`** (تُزاح بها المسافة بين العدّاد والمعلومات — كل +120px توسعة يعني +60 لكل جهة).
- **حدّ فني مهم:** لا نُنزل `max-width` تحت ~1000px بدون تضييق عمود المعلومات، وإلا **تنكسر الأعمدة** (العدّاد ينزل سطراً والبطاقة تنتفخ). حللنا التضييق بجعل صف أرقام السهم سطرين (`.scard-info { max-width: 300px }`) و`.scard-mid { flex: 1 1 220px }` (basis صغير يمنع لفّ الأعمدة).
- **الأبعاد الحالية:** ارتفاع البطاقة عبر `padding: 54px 14px` • البرواز المحيط `.screener-cards { border: 6px; padding: 50px }` • «الطول = العمودي، العرض = الأفقي» (مصطلح متفق عليه مع أحمد).
- **مصطلح متفق:** الطول = الارتفاع (فوق↔تحت)، العرض = الأفقي (يمين↔يسار).

**2026-07-13 (🌡️ ميزان الإشارات الفنية — ميزة جديدة بطلب أحمد من صورة TradingView):**
- مؤشر نصف دائري بـ**5 مناطق** (سلبي→محايد→إيجابي) وإبرة متحركة. قالب `templates/_techmeter.html` (macro `techmeter(frac)`).
- **تعليمي فقط** (اختار أحمد النبرة التعليمية): «ميل إيجابي/سلبي/محايد» بدل «اشترِ/بع» — يحافظ على مبدأ المنصة ويحميه قانونياً. (رفضنا نسخة «شراء/بيع».)
- **الحساب:** `screener.tech_tilt(record)` → `frac = (bull + 0.5×neutral) / total` من المؤشرات الفنية التسعة. مُسجَّل كـjinja global.
- **مكانان:** أعلى قسم المؤشرات بصفحة السهم (`meter` من مسار stock_report، مع تفصيل الأعداد) • **وبجوار عدّاد قوة التأكيد على كل بطاقة** (`.scard-meter` + كلمة الحالة الملوّنة + «الإشارات الفنية»).
- **الفرق عن قوة التأكيد** (سؤال أحمد): قوة التأكيد = كل الأدلة (16 عامل: فني+مالي+سيولة+نمو)؛ الميزان = المؤشرات الفنية فقط — فقد يختلفان، وهذا مقصود.
- الإبرة **دقيقة** (تعكس الرقم داخل التصنيف) لا تستقر بمنتصف المنطقة — قرار أحمد.
- **تنحيف القوس:** قلّلنا سماكة قوس كل العدّادات إلى `stroke-width: 3` (كان 8/9).
- **آخر CSS: `v=20260713a`**.

**2026-07-13 (مراجعة شاملة + عدّاد استهلاك FMP):**
- **مراجعة عامة للمنصة:** الخلاصة أن المنصة ناضجة ومكتملة جداً؛ الأولوية القادمة ليست ميزات أكثر بل **جولة تثبيت** للتجهيز لنمو المشتركين. أهم 3 أفكار طُرحت:
  1. 🔴 **خطر حدود FMP عند نمو المشتركين** (الأخطر تجارياً): كل فتح تقرير سهم يستهلك طلب FMP للسعر اللحظي؛ عشرات المشتركين قد ينفدون الحصة قبل الظهر. الحل المقترح: عرض السعر من آخر تحديث ليلي مع طابع زمني، وحصر السعر اللحظي بأزرار محدودة → يقطع ~90% من الاستهلاك.
  2. 🟡 **عدّاد استهلاك FMP** (نُفّذ — انظر أدناه).
  3. 🟡 **تنظيف الملفات:** ملف `حالة المشروع.txt` قديم ومتناقض (يقول Railway لم يُنشر) — يُحذف أو يُحدّث. + فكرة اختبار خفيف يتأكد أن كل الصفحات ترجع 200 قبل الدمج (يحمي الموقع الحي).
- **✅ نُفّذ: عدّاد استهلاك FMP** في لوحة «صحة المنصة» (بطاقة «🔌 استهلاك FMP اليوم» = X/250، أخضر <60% · أصفر 60–90% · أحمر ≥90%، يتصفّر 00:00 UTC).
  - `fmp_client.py`: `_record_call()` يزيد عدّاداً يومياً في `AppSetting['fmp_calls:YYYY-MM-DD']` عند كل طلب يصل خادم FMP. يستخدم **جلسة DB منفصلة** فلا يتداخل مع معاملة المتصل، وأي فشل يُتجاهل بصمت (تشخيصي فقط). + `get_today_usage()` للقراءة و`DAILY_LIMIT=250`.
  - اختُبر محلياً (العدّاد يزيد صحيحاً + العزل مؤكَّد: لا يلتزم بيانات المتصل). بلا CSS جديد (إعادة استخدام `.health-card`).
  - **الحالة:** على فرع `claude/american-platform-file-19r593` — **لم يُدمج في main بعد** (بانتظار قرار أحمد بالدمج/فتح PR).
- **⏭️ الخطوة القادمة (نقاش مفتوح):** بعد الدمج، نراقب رقم العدّاد يوماً–يومين → إن كان الاستهلاك مرتفعاً ننفّذ **حماية حصة FMP** (فكرة #1 أعلاه)؛ وإلا فالوضع مريح.

**أفكار مؤجّلة:** حماية حصة FMP (عرض سعر آخر تحديث ليلي بدل اللحظي) • تنظيف/حذف `حالة المشروع.txt` القديم • اختبار خفيف «كل الصفحات ترجع 200» قبل الدمج • ترقية باقة FMP عند زيادة المشتركين الدافعين • (اختياري) تلغرام خاص لكل مشترك لتنبيهاته السعرية • «تصنيف المحلّلين» في الميزان (يحتاج بيانات محلّلين خارجية — غالباً محجوبة بالباقة المجانية).
